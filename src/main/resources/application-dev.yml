spring:
  datasource:
    url: jdbc:postgresql://networking-db-postgres:5432/networking-micro-db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true